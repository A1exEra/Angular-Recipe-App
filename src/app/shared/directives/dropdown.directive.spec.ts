import { DropdownDirectiveTsDirective } from './dropdown.directive';

describe('DropdownDirectiveTsDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownDirectiveTsDirective();
    expect(directive).toBeTruthy();
  });
});
